const config = {
  firebase: {
    apiKey: "mock_apiKey",
    authDomain: "mock_authDomain",
    projectId: "mock_projectId",
    storageBucket: "mock_storageBucket",
    messagingSenderId: "mock_messagingSenderId",
    appId: "mock_appId",
  },
};

export default config;